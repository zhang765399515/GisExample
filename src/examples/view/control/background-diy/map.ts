import { Color,SkyBox } from "geokey-gis"
export function backGroundImg(){
	window.viewer.scene.skyBox.show = false;
    // window.viewer.scene.backgroundColor = Color.fromCssColorString("rgba(0,0,0,0)");
	// window.viewer.scene.globe.showGroundAtmosphere = false;
	// window.viewer.scene.globe.enableLighting = false;

}